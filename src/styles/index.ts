import { createStitches } from "@stitches/react";

export const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      yellow: "#FACC14",
      hYellow: "#F0C105",
      green: "#4ADE80",
      blue: "#60A5FA",
      black: "#131313",
      white: "#FFFFFF",
      gray: "#F2F3F5",
      darkGray: "#787878",
      blackGray: "#515151",
    },
  },
});
